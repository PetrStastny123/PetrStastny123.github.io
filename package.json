{
  "name": "petrstastny123.github.io",
  "version": "1.0.0",
  "description": "Personal portfolio website",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rimraf dist && mkdir dist && cp -r src/* dist/"
  },
  "keywords": ["portfolio", "personal website"],
  "author": "Petr Šťastný",
  "license": "ISC",
  "devDependencies": {
    "rimraf": "^5.0.0"
  }
} 